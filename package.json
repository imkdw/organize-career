{
  "name": "organize-career",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "node --env-file-if-exists=.env --import tsx src/index.ts",
    "clean": "node --import tsx src/clean.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
